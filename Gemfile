source "https://rubygems.org"

# GitHub Pages-compatible Jekyll. `github-pages` pins versions to match
# what GitHub's servers run, so local builds match the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-seo-tag"

# Windows/macOS compatibility helpers
gem "webrick"
